subscription DeviceAuth($deviceId: String!) {
  deviceAuth(deviceId: $deviceId) {
    accessToken
    refreshToken
    idToken
    username
    deviceId
  }
}



mutation addDevice(
  $accessToken: String!
  $deviceId: String!
  $idToken: String!
  $refreshToken: String!
  $username: String!
) {
  addDevice(
    input: {
      request: {
        accessToken: $accessToken
        deviceId: $deviceId
        idToken: $idToken
        refreshToken: $refreshToken
        username: $username
      }
    }
  ) {
    deviceCodeSubscriptionDto {
      accessToken
      deviceId
      idToken
      refreshToken
      username
    }
    errors {
      ... on ProblemDetailsError {
        code
        details
        message
      }
    }
  }
}
